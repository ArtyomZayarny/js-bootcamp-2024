"use strict";

// Strict mode highlighting undefined variables
let hasDriversLicense = false;
const passTest = true;

if (passTest) hasDriverLicense = true; // Variable with typo
if (hasDriversLicense) console.log("I can drive :D");

//Strict mode highlighting reserved variables names
const interface = "Auto";
